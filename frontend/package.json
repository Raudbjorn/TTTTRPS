{
  "name": "frontend",
  "version": "1.0.0",
  "description": "TTRPG Assistant Frontend",
  "private": true,
  "scripts": {
    "css": "./tailwindcss -i input.css -o public/main.css --minify",
    "css:watch": "./tailwindcss -i input.css -o public/main.css --watch"
  }
}
